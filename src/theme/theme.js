// Light Theme
export const lightTheme = {
    primary: "#ffffff",             // Background chính (màu trắng)
    secondary: "#f0f0f0",           // Background phụ (màu xám nhạt)
    surface: "#ffffff",             // Background cho các phần tử nổi (như card)
    textPrimary: "#000000",         // Màu chữ chính (màu đen)
    textSecondary: "#333333",       // Màu chữ phụ (màu xám đậm)
    border: "#d0d0d0",              // Màu viền
    button: "#007bff",              // Màu nút chính
    buttonHover: "#0056b3",         // Màu nút khi hover
    accent: "#6200ea",              // Màu nhấn cho các thành phần đặc biệt
};

// Dark Theme
export const darkTheme = {
    primary: "#131313",             // Background chính (màu đen xám)
    secondary: "#2e333d",           // Background phụ (màu xám đậm)
    surface: "#1e1e1e",             // Background cho các phần tử nổi (như card)
    textPrimary: "#e0e0e0",         // Màu chữ chính (màu trắng nhạt)
    textSecondary: "#a0a0a0",       // Màu chữ phụ (màu xám nhạt)
    border: "#3c3c3c",              // Màu viền
    button: "#6b8afd",              // Màu nút chính (như bạn yêu cầu)
    buttonHover: "#5A74D0",         // Màu nút khi hover
    accent: "#BB86FC",              // Màu nhấn cho các thành phần đặc biệt
};
